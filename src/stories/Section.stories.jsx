import React from "react";
import { Section } from "../shared/components/Section";

export default {
  title: "Components/Section",
};

export const SectionDefault = () => (
  <Section width={[1, 1 / 2]} height={"100px"} />
);
